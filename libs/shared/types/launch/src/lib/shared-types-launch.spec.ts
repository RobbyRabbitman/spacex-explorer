import { sharedTypesLaunch } from './shared-types-launch';

describe('sharedTypesLaunch', () => {
  it('should work', () => {
    expect(sharedTypesLaunch()).toEqual('shared-types-launch');
  });
});
