import { sharedTypesCommon } from './shared-types-common';

describe('sharedTypesCommon', () => {
  it('should work', () => {
    expect(sharedTypesCommon()).toEqual('shared-types-common');
  });
});
