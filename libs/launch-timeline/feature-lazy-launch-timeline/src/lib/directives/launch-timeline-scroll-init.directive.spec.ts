import { LaunchTimelineScrollInitDirective } from './launch-timeline-scroll-init.directive';

describe('LaunchTimelineScrollInitDirective', () => {
  it('should create an instance', () => {
    const directive = new LaunchTimelineScrollInitDirective();
    expect(directive).toBeTruthy();
  });
});
